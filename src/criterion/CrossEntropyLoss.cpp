#include "CrossEntropyLoss.h"

double CrossEntropyLoss::Forward(Eigen::MatrixXd &input, Eigen::MatrixXd &target) {
//   output_ = -1 /
    return 0.0;
    //todo
}

Eigen::MatrixXd CrossEntropyLoss::Backward(Eigen::MatrixXd &input, Eigen::MatrixXd &target) {
    return Eigen::MatrixXd();
    //todo
}
